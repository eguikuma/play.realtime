"use client";

import { Landing } from "@/features/room";

/**
 * トップページのエントリー、ルームの新規作成と URL 貼り付けによる入室を 1 画面でまとめた `Landing` をそのまま描画する
 */
export default function HomeEntry() {
  return <Landing />;
}
